import React from "react";
import { Text, View } from "react-native";

const GiftCards = () => {
  return (
    <View className={"flex-1 justify-center items-center"}>
      <Text className={"text-4xl text-stone-900 font-bold"}>Banking</Text>
    </View>
  );
};

export default GiftCards;
